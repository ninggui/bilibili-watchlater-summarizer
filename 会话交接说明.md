# B站「稍后再看」自动总结 · 完整会话交接说明

> 用途：把本项目已部署的工作流、规则、凭证与历史经验完整交接给新会话，让新会话无需重新摸索即可接手。
> 用户 UID: 228407179 · 飞书账号: 老实人（已绑定）· 项目主文档为飞书云文档（实时协作）。

---

## 一、任务全景（一句话目标）

自动把用户 B站「稍后再看」里的视频，逐条生成高质量中文总结写入飞书云文档，保持与 B站网页**严格同序**（方便对照），用户在文档里勾选 todo 后自动"双删"（删文档条目 + 删 B站稍后再看对应视频），所有规则与工作流沉淀为 Skill 并同步到 GitHub 发布仓，供跨平台复用（Hermes/NAS 等）。

## 二、核心凭证（当前有效）

### B站 Cookie（2026-09-20 导出，读写正常）
```
SESSDATA=52819ffd%2C1801827161%2C36fba%2A82CjAh5XLUnP7P8qhfdV-2Jhk1xtyxR1eSPojhuczVpjiz0Isdoh5t7xC_KzZpK3r6EpkSVmEyREdRVWZLNDZoSXo2RC0wSVNmRlBtUGJzeXZmWWdQRHF2RExQXzhmNUhWSGpja3FmdE00bHQ3THMyMW9GRW00Vk9udDlOdjJtX0J1WE9yNHZsVHJBIIEC; bili_jct=6a1aacb23885d6e2a2a31d5b7576a112; buvid3=C6C00CED-0F85-62A3-0E1D-9628A5D6143761240infoc; DedeUserID=228407179
```
- csrf（bili_jct）= `6a1aacb23885d6e2a2a31d5b7576a112`
- Cookie 通过环境变量传入脚本，禁止写进代码或提交 GitHub。

### 飞书文档（主产物，实时协作）
- token: `UmArd4W7Ios9NAxrislcc5OQn1g`
- URL: https://feishu.doubao.com/docx/UmArd4W7Ios9NAxrislcc5OQn1g
- 每次写操作前必须先 `lark-cli docs +fetch` 拿最新 block ID；写后 block ID 会变，必须重新 fetch 才能继续定位。

### GitHub 发布仓
- 主仓（非 git 仓库）: `bilibili-watchlater-summarizer/`（含 SKILL.md、scripts/、prompts/）
- 发布仓: `release-bilibili-watchlater-summarizer/`（git 仓库，branch=master，remote=github.com/ninggui/bilibili-watchlater-summarizer.git）
- 同步方式: 改主仓 SKILL.md → `cp` 到发布仓 `docs/SKILL.md` → `git add -A && git commit && git push origin master`

## 三、B站 API 要点（全部验证可用）

| 用途 | 接口 |
|---|---|
| 拉取稍后再看 | `GET https://api.bilibili.com/x/v2/history/toview?pn=&ps=100`（当前约 447 条） |
| 删除稍后再看 | `POST https://api.bilibili.com/x/v2/history/toview/del`，data=`{aid, csrf}`（aid 由 toview 列表的 `aid` 字段提供，先拉全量建 bvid→aid 映射） |
| BV→标题/UP主 | `GET https://api.bilibili.com/x/web-interface/view?bvid=` |
| 字幕 | `GET https://api.bilibili.com/x/player/wbi/v2?bvid=&cid=` → `data.subtitle.subtitles[0].subtitle_url`（**补 https: 前缀**）→ JSON `body[].content` 拼接 |

防 ban：脚本内置 random 0.55–0.95s 延迟；大批量操作控制节奏，单次拉取 100/页。

## 四、工作目录（persistent，勿依赖 /tmp）

根目录：`.../agents/m_0cwpvjR8GuN/owners_task/`（实际以会话工作区为准）
- `pairs.json`（516 条 title/bv 全量映射）
- `titles.json`（515 唯一 BV → 真实 title+owner，标题校验权威对照源）
- `long_short.json` / `sub_status.json` / `subinfo.json`（36 条长视频清单与字幕状态）
- `subs/{num}.txt`（32 条字幕原文）、`summaries/{num}.xml`（32 条详写总结 XML）
- `notes4/{num}/`（4 条妙记转写：536/537/538/555，含 MP3/MP4/transcript.txt）
- `doc_final.xml` / `doc_qc_final.xml` / `doc_now.xml` 等（阶段全文快照，含 block id）
- `sec694.json`、`del_plan*.json`、`dones_*.json`（分区快照与双删计划）

## 五、完整工作流（5 步）

### 第 1 步：取数 + 零成本分流（不花模型 token）
拉 toview 全量 → 用分区/时长/标题/字幕轨做路由决策：
- **subtitle**：有字幕且质量 good → 直接用字幕
- **transcribe**：无字幕 → 走飞书妙记转写（音频 MP3 上传，禁传视频）
- **card_meta / skip**：纯音乐/花艺/风光/带货/30 秒短视频 → 一句话带过，不转写
- 长视频（>10min）用带时间戳字幕；正文按 5-8 段详写；中视频 3-5 段；短视频 3-4 句

### 第 2 步：字幕抓取与防串台
- 必须用 **wbi 签名接口**（`x/player/wbi/v2`），未签名会返回串台字幕
- 取完整字幕（不设行数上限）；字幕 <100 字自动判为音乐/纯视觉类
- **字幕内容相关性校验**：字幕前 200 字与标题做关键词重叠（至少 1 个 ≥4 字关键词或标题核心名词），不相关 → 标记字幕串台，走妙记转写

### 第 3 步：妙记转写兜底（无字幕时）
- 用 `doubao-video-extract` skill 的 `social_video_to_minutes.py "<url>" --run-lark --notes-retries 0`（提交后手动轮询 `lark-cli vc +notes --minute-tokens <token> --output-dir ...`）
- 妙记本身不耗模型 token，仅摘要文本消耗（实测约 ¥0.001-0.002/条，投入产出比极高，用户已拍板"统一允许转写"）
- 转写完成（transcript.txt 就绪）→ 必须立即生成详写总结回填正文，禁止占位符滞留

### 第 4 步：AI 生成总结 + 写飞书
- 每条目结构（严格）：
```
<checkbox done="false">N. 视频标题</checkbox>
<p><em>频道/UP主 · 时长 · 分区　博主名</em>　</p>
<p>正文第1段</p>...<p>正文第N段</p>
<p>🔗 https://www.bilibili.com/video/BVxxx</p>
<hr/>
```
- **顺序严格对齐稍后再看原序**，不按密度重排；多 P 连续剧保持相邻；同主题不挪位
- em 元信息带博主名（格式如上，博主名=BV 真实 owner，禁止 ERR/留空）
- 信息差/新闻类（标题含"信息差/一觉醒来"）必须逐条列出全部条目，不得只列前 3 条
- 标题含"N款/N个"时总结必须覆盖至少 N 个独立要点

### 第 5 步：todo 双删（用户已授权：列清单后无需再确认直接执行）
1. fetch 文档全文，找 `done="true"` 的 checkbox
2. 文档侧：删除该条目 checkbox→hr 之间**所有 block**（含 p/ul/li，逐块删除最稳——含 ol/ul 容器时区间删除会跨容器失败，需从后往前逐 block 删）
3. B站侧：拉全量 toview 建 bvid→aid 映射，POST del
4. 删除后更新对应 h2 分区标题为**实际编号 min–max**（如"第 164–283 条"）
5. 检查排版：无孤儿段、无连续 hr、无 h2 后紧跟 hr

## 六、全部既定规则（用户多轮拍板，必须遵守）

1. **质量优先**：token 节约以质量为前提；长视频允许加字数，优先保证总结信息密度
2. **纯音乐/花艺/风光/带货/短视频**：一句话带过（例："花艺短视频：用三种花材插好一份周花，快速展示插花过程，无实质解说内容。"），不浪费 token
3. **妙记转写**：无字幕统一走妙记，允许转写；实测投入产出比高
4. **文档顺序**：严格对齐稍后再看原序，方便对照 B站网页
5. **em 博主名**：每条分类后标注博主名称（`频道 · 时长 · 分区　博主名`）
6. **单文档持续更新**：只保留一个主文档，旧版本删除
7. **运行频率**：由用户发起，不设定时
8. **GitHub 同步**：规则修改后必须同步发布仓
9. **跨平台复用**：方案封装为 Skill，可在 Hermes/NAS 等环境复用（用千问/其他模型也兼容——核心脚本仅依赖 requests）
10. **todo 双删**：勾选即删（文档+B站），无需再确认
11. **误删保护**：删除非实时，用户白天勾选、集中时间批量处理

## 七、已修复问题与根因（防重复踩坑）

| 问题 | 根因 | 修复规则 |
|---|---|---|
| "⚠️ 未匹配到BV号，需手动补全" | 源数据 BV 为空/写错；事后补 BV 依赖标题匹配失败 | BV 生成时直接写入，不依赖事后匹配；匹配失败走 B站搜索 API 兜底 |
| 链接系统性错位（696–716 整段"链接滞后一条"） | 批量写入时链接与标题逐条错位，抽查发现不了 | **全量错位审计**：每条 checkbox 标题 vs BV 真实标题相似度对比（阈值 0.25+主干包含），修复后重跑至 0 残留 |
| 标题变体误报（420/463） | UP 主改标题 | 共享核心实体（书名/主角名/BV 一致）判"标题变体非错位"，不修改 |
| 字幕串台（453 泰式广告配世田谷案字幕、310 麒麟配核能科普） | 未签名接口返回串台字幕 / 字幕索引错位 | wbi 签名 + 字幕内容相关性校验 + 标题-BV 一致性校验 |
| 无正文/占位符滞留 | 批量中断、短视频按规则省略但留空 | 短视频必须写一句话正文；占位符不得滞留，妙记完成后立即回填 |
| 无字幕直接标"无法总结" | 早期规则无兜底 | 统一走飞书妙记转写兜底 |
| em 博主名串台/ERR | 与链接错位同源 | em 博主名=BV 真实 owner，全量对齐 |
| 长视频正文过短 | 早期按模板压缩 | 长视频（>10min）5-8 段详写，覆盖字幕首尾，禁止截断（只写前 1/3 判定 truncated 重写） |
| 妙记转写中正文未回填 | 转写完成未触发回填 | 转写完成→立即 block_replace 回填正文 |

## 八、当前状态（2026-09-25）

- 飞书文档：421 条（编号 164–717），6 个分区：164–283 / 330–400 / 401–500 / 501–600 / 601–700 / 701–717
- 36 条长视频已全部详写回填（32 字幕 + 4 妙记 536/537/538/555）
- 链接错位残留：0（420/463 为标题变体非错位）
- 占位符：0；缺正文：0；缺 BV：0；em 缺博主名：0
- GitHub 已推送最新规则（commit e939cb8）
- B站稍后再看当前约 447 条，文档已覆盖编号 164–717（早于 164 的已读删）

## 九、新会话接手清单

1. 读取本说明 + 重读 `bilibili-watchlater-summarizer/SKILL.md`（含全部防错规则）
2. `lark-cli docs +fetch` 主文档拿最新状态（实时协作，用户可能又勾选 todo）
3. 若用户勾选新 todo：执行第 5 步双删
4. 若有新增视频：执行第 1-4 步增量生成
5. 任何规则修改：同步 GitHub 发布仓
6. 用 `titles.json` + 全量错位审计做质检，保证"通篇不再复发"

---

*交接时间：2026-09-25 · 由主会话生成*

## 十、多会话协作公约与监督机制（2026-09-25 新增，所有会话必须遵守）

> 本任务可多会话并行，任何会话对机制的修改必须收敛到单一真源，禁止各自为政。

**真源划分**
- 规则/数据/脚本（SKILL.md / config/skip_ups.json / CHANGELOG.md / scripts/）→ **GitHub 发布仓**（唯一权威，本地只是缓存）
- 运行状态（todo 勾选、正文、分区）→ **飞书主文档**（实时协作）
- 敏感凭证（Cookie）→ 飞书私密文件，不落 GitHub

**开工 5 步**（每个会话开始任务前必须执行）：
1. `git pull` 或重读 GitHub raw SKILL.md 拉取最新规则
2. `lark-cli docs +fetch` 飞书主文档拿最新状态
3. `python3 scripts/sync_check.py <仓库根>` → 本地 vs GitHub 差异必须全绿，有漂移先对齐
4. 读 `CHANGELOG.md`：查看其他会话最近改动
5. 向用户汇报"已对齐到最新"再开工

**收工 3 步**（每次修改后必须执行）：
1. 修改的规则/数据/脚本 `git push`（禁止只留本地）
2. `CHANGELOG.md` 追加一行：`日期 | 会话 | 改了什么 | 为什么`
3. 重大改动 → 同步更新本《交接说明》

**单会话写锁**：同一时间只允许一个会话执行"写飞书文档"流程；写前 fetch 最新 block ID；同一规则当天只允许一个会话改。

**监督机制**：每次开工跑 `sync_check.py`，结果记入 CHANGELOG「协议执行监督」表；发现漂移立即修正并记录，不得带病干活。

*更新：2026-09-25 · 多会话协作公约落地（commit 7275c9b）*
